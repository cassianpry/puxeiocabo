generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Fighter {
  shortId      BigInt  @id @unique
  fighterId    String?
  platformId   Int
  platformName String
  platformTool String
  circleName   String?
}
